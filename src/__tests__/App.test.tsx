import { assert } from "console";
import React from "react";
import ReactDOM from "react-dom";
import { App } from "../App";

it("Component tree renders without errors", () => {
  const div = document.createElement("div");
  //ReactDOM.render(<App />, div);
  assert(true);
});
